/*

            TYPE                                            DRIVING FORCE

SubArray sum/average related:                           window sum/average condition

Longest substring with k distinct chars:                use hashmap size comparison

                                                        // remember the maximum length so faR
                                                        maxLength = Math.max(maxLength, windowEnd - windowStart + 1)

Longest substring, which has no repeating characters:   use hashmap contains



 */
